# Liquid (.liquid) -- template Shopify/Jekyll.
# I commenti sono {% comment %}...{% endcomment %}, non una coppia di
# delimitatori a se': ricadono quindi nel blocco {% %} generico.
extensions = liquid
filetype = Liquid
base_tokenizer = xml
template_delimiters = {{ }}, {% %}

# Jinja2 (.jinja/.jinja2/.j2) -- template Python (Flask, Ansible, ...).
# Stessa forma di Nunjucks: HTML con delimitatori {{ }} / {% %} / {# #}.
extensions = jinja,jinja2,j2
filetype = Jinja
base_tokenizer = xml
template_delimiters = {{ }}, {% %}, {\# \#}
